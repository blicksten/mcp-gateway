# MCP Gateway — Roadmap

## Released

| Version | Date | Description |
|---------|------|-------------|
| v1.0.0 | 2026-04-09 | Go daemon + CLI (mcp-ctl) + VS Code extension + REST API + installers + cosign signing |
| v1.0.1 | 2026-04-14 | Security hardening: env key blocklist bypass fix, CRLF validation, URL validation, config permissions, MaxServers enforcement, disable-stops-process |
| v1.1.0 | 2026-04-16 | Extension UX polish (Phase 11): diff-based tree refresh, inline start/stop/restart, sidebar detail webview, MarkdownString tooltips, Add Server / Add SAP webview forms, SAP grouping toggle, slash-command generation |
| v1.2.0 | 2026-04-18 | Bearer token auth on daemon REST/SSE/MCP + mcp-ctl + extension GatewayClient/LogViewer (Phase 12.A); KeePass credential push via `mcp-ctl credential import --json --password-stdin` (Phase 12.B); Windows DACL token file |
| v1.3.0 | 2026-04-18 | POSIX process groups, config watcher race fix, TLS support with non-loopback refusal, log redaction pipeline (Phase 13) |
| v1.4.0 | 2026-04-18 | SECURITY.md responsible disclosure, gitleaks in CI, README security section (Phase 14) |
| v1.5.0 | 2026-04-20 | **Catalogs track** — first-party server+command catalogs (JSON schemas draft-07 $id-pinned, 5+5 seeds, TOCTOU-safe TS loader, 1 MiB cap), Add Server "Choose from catalog" dropdown with host-side re-validation, slash-command template enrichment via `${var}` allow-list substitution, `mcpGateway.catalogPath` machine-scope override. **v15 tail** — ConstantTimeCompare pad-to-expected hygiene refactor, `bufio.Scanner` 1 MiB cap on both log paths (F-11 closed), TLS integration tier + half-configured TLS refusal (breaking-config), Windows DACL OS-enforcement integration test + `make test-integration-windows` manual protocol. |
| v1.6.0 | 2026-04-22 | **Claude Code Integration (Phase 16)** — dual-mode gateway (`/mcp` aggregate + `/mcp/{backend}` per-backend proxy); Claude Code plugin packaging with auto-regen `.mcp.json`; `mcp-ctl install-claude-code` bootstrap CLI (flags `--mode|--scope|--no-patch|--dry-run|--refresh-token|--check-only`; exit codes 0/1/2/3/4); webview patch under **Alt-E** pattern (native `session.reconnectMcpServer` fiber walk at depth 2, no `executeCommand` or `extension.js` patch, opt-in); `gateway.invoke` universal fallback tool + `gateway.list_servers` / `gateway.list_tools` meta-tools; supported-versions map served via `GET /api/v1/claude-code/compat-matrix`; dashboard "Claude Code Integration" panel with 12-mode failure matrix + Alt-E metrics diagnostics; 8 REST endpoints under `/api/v1/claude-code/*` (FROZEN v1.6.0 contract); slash-command 3-line disclaimer + regression-pinned test; dogfood-smoke CI; ADR-0005. |

Phases 1–16 implemented. Full history preserved locally in `full-history-backup` branch.

---

## Operator-facing analysis

- `docs/spikes/2026-04-24-scalability-vs-classical-mcp.md` — multi-window scalability vs classical stdio MCP (resource math for 3 windows × 16 tabs, 4-layer concurrency analysis, orchestrator/PAL disconnect-resilience, hybrid-mode recommendations). Summary section in `docs/ANALYSIS.md`.
- `docs/spikes/2026-04-14-toolc-mcp-gateway-analysis.md` — context-window cost / staged tool loading (complementary; different axis).
- `docs/spikes/2026-04-20-reload-plugins-probe.md` — Claude Code webview reconnect-mcp-server probe (Phase 16.0 gate, live DevTools verification).
- `docs/spikes/2026-04-19-windows-latest-impersonate.md` — Windows DACL enforcement tier (Phase 15.C).

---

## Phase 17 — Dashboard UX Polish Round 2 (COMPLETE, v1.7.2 in local `main`)

Detailed plan: `docs/PLAN.md` (2026-04-22, CV via PAL thinkdeep + Sonnet code-reviewer fallback).

Delivered in one v1.7.0 bundle:

| # | Sub-phase | Status |
|---|-----------|--------|
| 17.1 | Remove `mcpGateway.serverDetail` sidebar view; keep modal-on-demand (`ServerDetailPanel`). Deleted `server-detail-view-provider.ts` + test. | ✅ implemented |
| 17.2 | Button audit matrix (31 rows). Cancel-button wiring in source is **correct** end-to-end (`html-builder.ts:307+500`, `add-server-panel.ts:148-151`). User report attributed to stale VSIX install — active instrumentation **deferred**, fresh install this round re-tests. | ⏸️ deferred (test with fresh VSIX first) |
| 17.3 | New command `mcpGateway.openSettings` → `workbench.action.openSettings('@ext:mcp-gateway.mcp-gateway-dashboard')`; gear icon on both tree title bars (navigation@90). | ✅ implemented |
| 17.4 | Explicit `navigation@10/@20/@90` priorities on view/title menus (prevents overflow collapse hiding Add); added `[Add SAP System](command:mcpGateway.addSapSystem)` link to `mcpSapSystems` viewsWelcome. | ✅ implemented |
| 17.5 | New config `mcpGateway.keepassEnabled` (bool, default false); new `synthesizeKeepassSapSystems` helper; `ServerDataCache(client, importedProvider)` 2-arg constructor; union-merge in `refresh()`; `SapSystem.imported` flag; `sap-imported` contextValue; imported-row rendering (cloud-download icon, explanatory tooltip). Config flip triggers immediate refresh. | ✅ implemented |

**CV record:** `mcp__pal__thinkdeep` at design time, `mcp__pal__codereview` fallback via Sonnet sub-agent at implementation time (PAL MCP was disconnected). 1 HIGH (provider exception must not crash refresh) + 2 MEDIUM (flatten merge guard, explicit imported-row short-circuit in `getChildren`) + 1 LOW (provider-throws regression test) — all fixed in-cycle.

**Test deltas (local daemon on :8765 intercepts HTTP → 31 failures are environmental, baseline-identical):**
- Baseline HEAD: 568 pass / 31 fail
- After 17.3/17.4: 570 pass / 31 fail (+2 new `openSettings` tests)
- After 17.1: 555 pass / 31 fail (−15 deleted `ServerDetailViewProvider` tests)
- After 17.5: 574 pass / 31 fail (+19 synthesize/union/imported-row tests)
- After CV fixes: 575 pass / 31 fail (+1 provider-throws regression test)

**Files touched:** `package.json`, `src/extension.ts`, `src/sap-detector.ts`, `src/server-data-cache.ts`, `src/sap-item.ts`, `src/sap-tree-provider.ts`, `src/test/mock-vscode.ts`, `src/test/commands.test.ts`, `src/test/sap-detector.test.ts`, `src/test/sap-item.test.ts`, `src/test/server-data-cache.test.ts`. Deleted: `src/webview/server-detail-view-provider.ts`, `src/test/webview/server-detail-view-provider.test.ts`.

**Orchestrator pipeline** `feature-4d1cda14` was halted mid-flight earlier this session due to PAL MCP / orchestrator disconnect. Work completed directly via the standard phase-by-phase loop with main-session verification. Cancelled during /finish orphan-scan.

**/check pass (2026-04-23):** architect + PAL `gpt-5.2-pro` thinking=high → [C+O] on 3 findings (F-1 LOW `localeCompare` locale, F-2 LOW config-refresh race, F-3 MEDIUM `sap-imported` context menu absent). All fixed in commit `fc76cbc` (v1.7.2):
- F-1 → shared `compareByName` using `Intl.Collator('en', {sensitivity:'variant', numeric:true})` in `sap-detector.ts`; `vsp-2` now sorts before `vsp-10`.
- F-2 → `pendingRefresh` flag in `ServerDataCache.refresh()`; drain runs once after in-flight, coalesces colliders, guarded by `disposed`.
- F-3 → two new `view/item/context` entries on `viewItem == sap-imported` (`showSapDetail` + `addSapSystem`) in `package.json`.
- +6 new tests (3 compareByName + 3 F-2 re-queue). Tests: 580 → 586 / 31 env-fail unchanged.

**/finish pass (2026-04-23):** orphan-scan cancelled 2 stale pipelines (`feature-4d1cda14` + `checkpoint-check-6753fbb6`). PAL `gpt-5.1-codex` precommit expert review on full 4-commit changeset → READY TO PUSH, zero findings at all severities. VSIX v1.7.2 built (579.92 KB, 585 files), installed.

**Commits on `main` (unpushed at /finish):**
1. `dffb4b3` v1.7.0 Phase 17 UX polish
2. `50d342f` v1.7.1 MCP alphabetical sort
3. `8f405f4` debug-flicker.1 cache preserve (parallel session)
4. `fc76cbc` v1.7.2 /check audit fixes (F-1 + F-2 + F-3)

---

## UX toggles (post-v1.7.x)

| # | Phase | Status |
|---|-------|--------|
| ux-sap-toggle | `mcpGateway.sapSystemsEnabled` (bool, default **false**, scope `window`). Hides the SAP Systems view + gates `SapTreeProvider`/`sapTreeView`/`SapStatusBar`/`SapDetailPanel.updateAll` construction. SAP commands stay palette-reachable as operator escape hatch. `onDidChangeConfiguration` updates the context key live + shows a "Reload Window" toast. Agent-sonnet CV fallback (PAL disconnected): 3 LOW findings (F-1 + T-1 + T-2) all fixed in-cycle. Tests 634 / 31 env-fail. VSIX v1.9.0 (591 KB / 587 files) installed. | ✅ implemented 2026-04-24 |

---

## MCP Lifecycle track (post-v1.13.0)

Plan: `docs/PLAN-mcp-lifecycle.md` · Reviews: `docs/REVIEW-mcp-lifecycle.md`. Investigative spike: `docs/spikes/2026-05-01-cc-v2.1.123-regression-investigation.md`.

| # | Phase | Status |
|---|-------|--------|
| 1–9 | Functional test campaign (44 cells across baseline / add / verify / visibility / stop-start / remove / re-add / UI sweep / compile) | ✅ closed 2026-04-30 (Phases 1, 2, 3, 5, 6, 7 — autonomous + operator-mediated) |
| 10 | Plugin user-config remediation (writePluginUserConfig + atomic merge + advisory lock; reverted speculative plugin.json + regen.go authToken inline; daemon Discover v2 layout glob) | PARTIAL (gateway-side complete; visibility blocked by upstream Claude Code v2.1.123 regressions) |
| 11 | **Architectural pivot — extension as MCP-config bridge** (workaround for F-CC-V2-PLUGIN-MCPSERVERS-NOT-LOADED + F-CC-V2-WEBVIEW-CSP-NO-CONNECT-SRC). New `vscode/mcp-gateway-dashboard/src/claude-config-sync.ts` subscribes to `ServerDataCache.onDidRefresh` and reflects gateway backend list into `~/.claude.json::mcpServers` under prefix `mcp-gateway:<name>` with HTTP type + Bearer header. **Concurrency:** sha256 CAS read-merge-write with 5-retry budget · atomic temp+rename with 5-retry on Windows EBUSY/EPERM · JSON.parse retry on transient mid-write SyntaxError · diff-only writes (canonicalized deep-equal). **Co-tenancy:** foreign keys preserved verbatim in original insertion order; gateway-as-truth cleanup drops orphans on each refresh. **Kill-switch:** `mcpGateway.claudeConfigSync.enabled` (default `true`) — flip off when Anthropic restores plugin loader to avoid duplicate `/mcp` panel entries. **Commands:** `mcpGateway.cleanupClaudeConfig` (modal warning + purge all managed) · `mcpGateway.previewClaudeConfigSync` (dry-run diff in untitled doc, non-mutating). **Tests:** 17 new in `src/test/claude-config-sync.test.ts` including the load-bearing inv-4A concurrent-writer cross-process simulation (5 rounds × 3 user entries via `child_process.spawn(node, [mutator.js])`, CAS-protected — all 15 user-* keys + final managed set preserved). VSIX v1.15.0. PAL `gpt-5.2-pro` thinking=high cross-validated approach (continuation expired; refresh consult landed before implementation). Empirical proof gathered before commit: `claude mcp list` re-reads `~/.claude.json` mid-session and connects through gateway-proxy with namespaced names `mcp-gateway:test-pal-mcp` ✓. | ✅ implemented 2026-05-01 |

---

## Audit-dashboard track (post-v1.9.1)

Plan: `docs/PLAN-audit-dashboard.md` (v3 APPROVED 2026-04-25 by PAL `gpt-5.2-pro` thinkdeep gate_mode=true). Reviews: `docs/REVIEW-audit-dashboard.md`.

12-phase track addressing **31 audit findings** (2 CRITICAL, 8 HIGH, 13 MEDIUM, 8 LOW) across six defect classes: zombie DOM elements, sentinel data stubs, cross-file contract drift, zombie state after upstream change, silent error swallowing, lifecycle/safety gaps. Discovered via two-pass audit (initial 17 + double-audit sub-agent +13 + PAL gpt-5.2-pro promotion of B-NEW-18 to CRITICAL and surfacing of B-NEW-31 as separate security concern).

| # | Phase | Status |
|---|-------|--------|
| 0 | **Contract-drift fixes + compat shim + canonical contract module** (closes B-NEW-18 CRITICAL env-var name drift in patch-installer chain, B-NEW-19 HIGH 401 misclassification as offline, B-NEW-23 MEDIUM Activate hardcoded URL, B-NEW-28 LOW patch-installer test coverage gap, B-NEW-31 CRITICAL token bytes via env security regression). 13 files: new `installer-contract.ts` (frozen INSTALLER_ENV/LEGACY_INSTALLER_ENV typed export); `patch-installer.ts` swaps `gatewayAuthToken: string` → `tokenPath: string` and emits MCP_GATEWAY_URL+MCP_GATEWAY_TOKEN_FILE+legacy GATEWAY_URL only (zero raw-bytes via env); `apply-mcp-gateway.{sh,ps1}` accept new + legacy URL with stderr deprecation warning; `gateway-client.ts` adds `kind:'auth'` for HTTP 401; `server-data-cache.ts` propagates `lastAuthFailed` only on `kind:auth` and clears on success; `extension.ts` shows one-shot toast with Reload Window action latched via `authErrorNotified`; `claude-code-panel.ts` `defaultSpawnInstall` argv now `['install-claude-code', '--api-url', deps.getGatewayUrl()]`; new `src/test/claude-code/patch-installer.test.ts` (10 tests); new `installer/patches/test-contract.sh` (bash-side env-contract smoke test); +new B-NEW-23 argv test in `claude-code-panel.test.ts`; +new B-NEW-19 401 → kind:auth test in `gateway-client.test.ts`; +new lastAuthFailed-only-on-kind:auth test in `server-data-cache.test.ts`. PAL `gpt-5.1-codex` codereview gate_mode=true verdict **PASS, 0 blocking findings** (3 advisory items recorded in REVIEW_FILE as out-of-scope: daemon-side `MCP_GATEWAY_AUTH_TOKEN` is a different active env var, gateway-client.test.ts legacy /api/* routes are pre-existing baseline-flaky bug, PowerShell apply-mcp-gateway.ps1 contract test deferred to follow-up). Scoped Phase 0 critical-path tests **52/52 passing**; full suite **647 passing / 31 baseline-flaky failing** (+47 net vs 599 baseline). Acceptance grep `git grep "GATEWAY_AUTH_TOKEN" -- vscode/` confirms zero live raw-bytes-via-env emissions. Pipeline `execute-2be14a13`. | ✅ implemented 2026-04-25 |
| 1 | **ClaudeCodePanel zombie elements + hardcoded facts** (closes B-05 CRITICAL gatherFacts hardcoded `pluginInstalled: true, patchInstalled: true` made A-H failure modes unreachable, B-01..B-04 HIGH `#pluginStatus`/`#patchStatus`/`#channelStatus`/banner zombie elements with no JS handler, B-13 LOW AutoReload toggle persistence). New `src/claude-code/detection.ts` exports three pure async helpers with 60s TTL cache: `detectPluginInstalled` spawns `claude plugin list --json` with 5s timeout and falls back to `{installed: false}` on every error path; `detectPatchInstalled` walks `~/.vscode/extensions/anthropic.claude-code-*` and reads webview/index.js to extract MCP Gateway Patch version vs bundled `porfiry-mcp.js`; `detectChannelStatus` returns `{state: 'unknown', detail: ...}` until probe-trigger telemetry lands in a future phase. `claude-code-panel.ts` `gatherFacts` now awaits real detection (replaces hardcoded TODO `true` literals); new `postFactsUpdate` posts `{kind:'facts-updated', plugin, patch, channel}` to webview after each tick; new `postBannerUpdate` posts three actionable banner-tone variants per Plan B-04 (gateway down red / sessions=0 plugin-installed restart-cc yellow / sessions=0 plugin-not-installed click-Activate yellow); HTML banner copy `Polling…` → `Checking…`; `ClaudeCodePanelDeps` grew optional `detectPlugin?/detectPatch?` injection hooks mirroring existing `spawnInstall?` pattern. New `src/test/claude-code/detection.test.ts` (12 tests covering all helper paths + cache TTL + _resetDetectionCache); 2 new tests in `claude-code-panel.test.ts` for facts-updated payload. PAL `gpt-5.1-codex` precommit internal gate_mode=true verdict **PASS, 0 blocking findings** (continuation b7f9e1c7-99b9-4666-9a18-d1f50ebb104b). Full suite **661 passing / 31 baseline-flaky failing** (+14 net vs Phase 0 baseline 647, 0 new regressions). Pipeline `execute-4cbfb1b7` orchestrator-side hit a spawn_token lease error mid-pipeline; commit landed via direct PAL precommit gate per plan-aware fallback (same pattern as Phase 17 / debug-flicker tracks). | ✅ implemented 2026-04-25 |
| 2 | **Shared Logger + remove silent catches** (closes B-08 MEDIUM 4 silent `.catch(() => {})` sites in extension/cache/slash-generator, B-09 MEDIUM OutputChannel 'MCP Gateway' was only written by daemon.ts, B-14 LOW no log trail for REST calls — `mcpGateway.verboseLogging` setting reserved for Phase 4 wiring, B-15 LOW detail panels had no error feedback path). New `src/logger.ts` (singleton lazy `OutputChannel('MCP Gateway')`, ISO-timestamp `[time] [LEVEL] [source] msg` format, four levels info/warn/error/debug, never-throws via `try/catch` around `appendLine`, `_setLoggerForTests`/`_getInternalChannel` exports for test injection, `debug` honours `mcpGateway.verboseLogging` boolean default false). All audit-flagged silent catches replaced with `logger.error(source, context, err)` in `extension.ts` (KeePass toggle), `server-data-cache.ts` (auto-refresh), `slash-command-generator.ts` (queued task chain). `daemon.ts` migrated from owned `OutputChannel` to shared `logger.*` calls. The 2 remaining `.catch(() => {})` sites in `extension.ts:207+210` retain post-dispose race absorber semantics with explicit comment `// post-dispose race absorber — see audit ADR for B-08 reassessment`. `webview/server-detail-panel.ts` + `sap-detail-panel.ts` `_render` wrapped in try/catch with `logger.error` + warning toast latched once per panel session (B-15). New `src/test/logger.test.ts` (12 tests covering format, levels, error.message extraction, debug-skip-when-off, channel injection, never-throw on appendLine failure). PAL `gpt-5.1-codex` precommit internal gate_mode=true verdict **PASS, 0 blocking findings** (continuation d0614391-57e7-46cf-936d-5704a4335688). Full suite **673 passing / 31 baseline-flaky failing** (+12 net vs Phase 1 baseline 661, 0 regressions). | ✅ implemented 2026-04-25 |
| 3 | **Backend-aware commands** (closes B-06 MEDIUM `mcpGateway.stopDaemon` silent no-op against externally-started daemon, B-07 MEDIUM `startDaemon` had no try/catch). `extension.ts:startDaemon` now `try { spawned = await daemon.start() } catch (err) { logger.error + showErrorMessage('Start daemon failed: ' + err.message) }` so spawn ENOENT/EACCES/PATH-missing surfaces a useful toast instead of generic command-failed error. `extension.ts:stopDaemon` rewritten REST-first: `client.getHealth().catch(() => null)` reachability probe; if null show "No daemon process to stop" info; else `client.shutdown()` — when reject is `GatewayError(kind:'auth')` show error toast hinting `mcp-ctl install-claude-code --refresh-token`, when `kind:'connection'` fall back to local `daemon.stop()`, else generic error; on success poll `getHealth` every 250 ms up to 5 s for unreachability then `await daemon.stop()` if `daemon.running` to clean up the local spawn handle. Tests: 5 new in `commands.test.ts` covering owned/external/unreachable/401/start-throw scenarios + tracking client extended with `shutdown()` recorder; existing `daemon.test.ts` stopDaemon integration test updated for REST-first ordering. PAL `gpt-5.1-codex` precommit internal gate_mode=true verdict **PASS, 0 blocking findings** (continuation 99e16f7e-a19f-4bc2-9968-b27141b42fd5). Full suite **678 passing / 31 baseline-flaky failing** (+5 net vs Phase 2 baseline 673, 0 regressions). | ✅ implemented 2026-04-25 |
| 4 | **gatewayVersion wiring + probe cleanup + version-skew guardrail** (closes B-10 MEDIUM `gatewayVersion: 'unknown'` placeholder in Copy Diagnostics, B-11 LOW dead 15-second poll loop in `handleProbeReconnect`). New `src/version-compat.ts` pure semver module: `parseSemVer` + `isAtLeast` + `assertCompatible` + `MIN_GATEWAY_VERSION = '1.5.0'`; fail-safe to compatible on malformed input. `claude-code-panel.ts` `ClaudeCodePanelDeps` grew optional `getGatewayVersion?(): string | undefined` injection hook; `handleCopyDiagnostics` now reads via `deps.getGatewayVersion?.() ?? 'unknown'`; `handleProbeReconnect` 15s sleep loop deleted, immediately toasts after probe POST (handler now < 1s). `extension.ts` adds `versionCompatChecked` + `lastCheckedDaemonVersion` latches; on first health fetch (or daemon hot-swap) calls `assertCompatible`; on `IncompatibleVersionError` shows error toast with remediation per PAL gpt-5.2-pro 2026-04-25 audit addendum. New `src/test/version-compat.test.ts` (10 tests); 3 new tests in `claude-code-panel.test.ts`. PAL `gpt-5.1-codex` precommit gate **PASS, 0 findings** (continuation 4b9f16d6-347e-4dfe-98ef-d53d71be5311). Full suite **708 passing / 31 baseline-flaky failing** (+30 net vs Phase 3 baseline 678, includes parallel-session claude-config-sync tests, 0 regressions). | ✅ implemented 2026-04-25 |
| 5 | **Activate-button CWD independence** (closes B-12 MEDIUM Activate button broke when VSCode workspace was outside the mcp-gateway repo because `defaultSpawnInstall` did not pass `cwd` to `execFile` and the extension inherited VSCode's process CWD). `ClaudeCodePanelDeps` grew two optional injection hooks: `getWorkspaceFolder?: () => string \| undefined` and `getMarketplaceJsonPath?: () => string \| undefined`. New private `getActivateSpawnOptions()` helper assembles `{cwd, envOverrides}` from those hooks (uses `\|\| undefined` to convert empty string to undefined — avoids the `cwd:''` bug; `envOverrides` only set when marketplacePath is truthy — no `GATEWAY_MARKETPLACE_JSON: undefined` leak). `defaultSpawnInstall` signature extended `(mcpCtlPath, argv, opts?: {cwd?, envOverrides?})`; spawn options apply `cwd` directly and `env: opts?.envOverrides ? {...process.env, ...opts.envOverrides} : process.env`. `extension.ts` wires `getWorkspaceFolder: () => vscode.workspace.workspaceFolders?.[0]?.uri.fsPath ?? undefined` (multi-root out of scope per plan) and `getMarketplaceJsonPath: () => cfg.get<string>('marketplaceJsonPath', '').trim() \|\| undefined`. New setting `mcpGateway.marketplaceJsonPath` (string, machine-scope, default empty). 4 new tests in `claude-code-panel.test.ts` covering cwd-from-getWorkspaceFolder, no-cwd-when-undefined, GATEWAY_MARKETPLACE_JSON-when-set, no-env-key-when-empty/undefined. PAL `gpt-5.1-codex` precommit gate **PASS, 0 findings** (continuation 9847001c-b92f-4f53-b06e-4642920beeba). Full suite **712 passing / 31 baseline-flaky failing** (+4 net vs Phase 4 baseline 708, 0 regressions). | ✅ implemented 2026-04-25 |
| 6 | **Manual QA checklist + zombie-DOM CI script** — landed 2026-05-01 v1.18.0. Three deliverables: `docs/qa/dashboard-smoke.md` 30-item 4-column matrix (gateway up / down / 401 / externally-started); `vscode/mcp-gateway-dashboard/scripts/check-zombie-dom.js` plain-JS CI scanner per CV-gate D-2 convention (mirrors `check-catalog-refs.js`), wired via `pretest`; `vscode/mcp-gateway-dashboard/src/test/check-zombie-dom.test.ts` 30 unit tests (id extraction × 9, script-ref detection × 10, end-to-end × 3, synthetic fixtures × 3, comment-stripping × 3, validator × 2). PAL `gpt-5.1-codex` codereview gate **PASS, 0 blocking findings** (2 HIGH advisories addressed in-cycle: single-quote ids + comment-stripping false positives → added `stripJsComments()` and dual-quote support, +7 new tests). Full suite **744 passing / 32 baseline-flaky** (LogViewer/GatewayClient timeout noise; +1 vs 31-baseline due to slower full-suite runtime, not a regression). Acceptance gate `check-zombie-dom: OK — src/webview/claude-code-panel.ts (13 ids, 12 wired, 1 allowlisted)` passes via `npm test` `pretest` hook. Commit `24d8c4c`. | ✅ implemented 2026-05-01 |
| 7 | **mcp-ctl doctor + ARCHITECTURE.md** (closes B-16 LOW no single-command diagnostic, B-17 LOW no architecture orientation doc). New `cmd/mcp-ctl/doctor.go` (~370 lines): 5 sequential checks (gateway reachable via REST GET /api/v1/health 3s timeout, auth token present + non-empty, claude CLI available, plugin installed via `claude plugin list --json`, webview patch applied via `anthropic.claude-code-*/webview/index.js` marker scan). Tabwriter PASS/FAIL/SKIP table with remediation hints; `--no-color` flag with TTY auto-detect via `os.ModeCharDevice`; exit 0 if green, 1 otherwise. Dependency injection: `commandRunner` (mirrors install_claude_code.go pattern), `httpClient`, `extensionsDir` (test override `$GATEWAY_VSCODE_EXTENSIONS_DIR`). New `cmd/mcp-ctl/doctor_test.go` 22 test funcs covering happy path + every failure branch + helpers. New `vscode/mcp-gateway-dashboard/ARCHITECTURE.md` ~50-line orientation doc (component table, tree-view table, REST surface, build commands, doctor pointer). PAL `gpt-5.1-codex` codereview gate_mode=true **PASS, 0 blocking findings**; expert raw_analysis surfaced 3 advisories (1 MEDIUM TTY auto-detect contract, 2 LOW lex-sort + duplicated marker) all Fixed in-cycle. Full mcp-ctl Go suite green (152+ tests, 18.843s); `go vet` clean. Pipeline `execute-02dd902e` step 5/7 PASSED with orchestrator-side CV gate SKIP=PAL_TIMEOUT (own in-session PAL had already PASSED — same pattern as Phase 0). | ✅ implemented 2026-05-01 |
| 8 | **Detail-panel reconcile + settings-change watcher** (closes B-NEW-20 HIGH detail panels keep showing removed servers, B-NEW-22 MEDIUM 4 of 7 settings ignored after activation). New `buildRemovedHtml(displayName, kind, nonce, cspSource)` helper in `webview/html-builder.ts` — disabled-button banner with proper CSP + nonce. `ServerDetailPanel` + `SapDetailPanel` gain `showRemoved()` (idempotent, replaces panel HTML, schedules `panel.dispose()` after `REMOVED_AUTO_CLOSE_MS=3_000`); `update()` short-circuits when removed; `updateAll()` calls `showRemoved()` on any tracked panel whose name/key is missing from the latest cache snapshot; `dispose()` clears the pending auto-close timer to prevent leaks. `extension.ts` adds a single unified `onDidChangeConfiguration` watcher gated on the 6 reload-required keys (apiUrl, pollInterval, autoStart, daemonPath, authTokenPath, mcpCtlPath); shows one info toast with a `Reload Window` action; the `reloadPromptShown` latch debounces a burst of changes but re-arms on dismissal so a later edit re-prompts. Tests: +9 ServerDetailPanel reconcile/showRemoved + XSS guard + nonce/CSP + setTimeout boundary; +5 SapDetailPanel mirror; +10 reload-watcher (6 keys × toast assertion + does-not-trigger-for-live-keys + debounce + reload-action wiring + re-arm-after-dismiss). **Bonus CI fix:** replaced stale `TestInstallClaudeCode_PluginSyncConflictTriggersRollback` in `cmd/mcp-ctl/install_claude_code_test.go:256` (was asserting 409 → rollback, but REVIEW-4B-F1 at `install_claude_code.go:39-48` intentionally treats 409 as a non-fatal warning — this stale test has been failing every CI run since the contract change) with two replacements: `TestInstallClaudeCode_PluginSyncConflictIsNonFatal` (pins new 409 contract: no error, no `claude plugin uninstall` call) + `TestInstallClaudeCode_PluginSyncFatalErrorTriggersRollback` (uses inline `httptest.Server` returning HTTP 500 on `/plugin-sync` to keep the rollback-path regression guard alive). Sonnet sub-agent fallback codereview (PAL MCP disconnected mid-pipeline; per CLAUDE.md fallback policy launched a different-tier Agent for cross-model review) → APPROVE, 0 blocking findings. Full extension suite **769 passing / 31 baseline-flaky failing** (LogViewer/GatewayClient timeout noise — same baseline as Phase 6/7). `cmd/mcp-ctl` Go suite green: `ok mcp-gateway/cmd/mcp-ctl 89.037s`, exit 0. VSIX v1.19.0. | ✅ implemented 2026-05-02 |
| 9 | Windows daemon-kill safety + REST-first stop (B-NEW-21, B-NEW-25) | ⏳ pending |
| 10 | Atomic writes + serialization races (B-NEW-24, B-NEW-26, B-NEW-27, B-NEW-30) | ⏳ pending |
| 11 | Perf: async token caching (B-NEW-29) | ⏳ pending |

---

## Debug patches (post-v1.7.1)

Plan: `docs/PLAN-debug-flicker.md` (locked 2026-04-23, PAL `gpt-5.1-codex` thinkdeep+chat APPROVED).

| # | Phase | Status |
|---|-------|--------|
| debug-flicker.1 | Fix cache flicker — `ServerDataCache.refresh()` now preserves last-known-good `cachedServers` on API errors (was wiped to `[]`). `_lastRefreshFailed=true` flag is kept so consumers still distinguish daemon-offline from genuinely empty lists. Fingerprint-based tree providers no longer re-fire during transient drops → no visible Backends/SAP Systems sidebar flicker. 21 `ServerDataCache` tests pass (added 3 new invariant tests + rewrote T3.10 cold-start). PAL `codereview` external expert gpt-5.1-codex → PASS, zero findings. | ✅ implemented 2026-04-23 |
| debug-flicker.2 | Cold-start placeholder — new `PlaceholderTreeItem` (`contextValue='gateway-connecting'`, icon `$(sync~spin)`, label "Connecting to gateway…") surfaces at root of MCP Backends + SAP Systems views when `cache.lastRefreshFailed && servers.length===0`. Fingerprints extended with `P/N` prefix so state transitions fire the tree event even when both sides are empty-list. `SapTreeProvider` generic widened from `SapTreeNode` to `vscode.TreeItem` per PLAN's TS contract note (subtype-compatible, no union). 43/43 tree-provider tests pass (10 new integration + 5 new class + updated H/F marker). Full suite 599/33 (delta +16 passing / 0 new failures vs Phase-1 baseline). PAL `codereview` + `thinkdeep` (both gpt-5.1-codex, external, gate_mode=true) → **dual-PASS, zero findings**. Menu-clause Grep audit confirms `gateway-connecting` does not match any `viewItem ==` / `viewItem =~` clause in `package.json` — placeholder renders inert. | ✅ implemented 2026-04-24 |
| debug-flicker.3 | Install Claude Code plugin end-to-end (operator steps). | ⏳ pending |
| debug-flicker.4B | Wire "Activate for Claude Code" button to `child_process.execFile('mcp-ctl install-claude-code')` with webview log streaming + cancel. | ⏳ pending |
| debug-flicker.Final | `npm run deploy` + PAL precommit + single commit bundling source + VSIX + plan/tasks/review artifacts. | ⏳ pending |

---

## Backlog

### Phase 11 — Extension UX (v1.1.0)

11.0 COMPLETE (`7fbd4c0` — status bar foreground tinting). 11.A COMPLETE (`8650505`, 2026-04-15 — fingerprint-based diff refresh, inline start/stop/restart icons, `ServerDataCache.lastRefreshFailed`). 11.B COMPLETE (`6d059e2`, 2026-04-15 — sidebar `WebviewView` detail, MarkdownString tooltips, `McpStatusBar` cache migration). 11.C COMPLETE (`ec3aae7`, 2026-04-15 — `AddServerPanel` webview form replacing sequential InputBox flow, shared `src/validation.ts` module, platform-agnostic `isAbsolutePath`, CSP `form-action 'none'`, in-flight submit guard, exception-safe dispose ordering). 11.D COMPLETE (`44f0d7a`, 2026-04-15 — SAP hierarchical tree via new `SapComponentItem` + `sapGroupBySid` setting + live config watcher, new `AddSapPanel` webview form with absolute-path VSP/GUI executable fields + Set-based duplicate-detection). 11.E COMPLETE (`991121f`, 2026-04-16 — `SlashCommandGenerator` with promise queue, magic-header marker, transition detection, orphan cleanup, daemon outage protection; opt-in via `slashCommandsEnabled` setting; 24 tests).

| # | Task | Description |
|---|------|-------------|
| 11.1 | ✅ Diff-based tree refresh | Only fire TreeView update when data actually changed — eliminate flicker and scroll jumps. |
| 11.2 | ✅ Inline start/stop/restart buttons | Show control icons directly on each server row, no context menu needed. |
| 11.3 | ✅ Sidebar detail webview | Always-on `WebviewView` beneath tree views; re-renders on selection + cache refresh. |
| 11.4 | ✅ Status bar + tree item MarkdownString tooltips | Per-server breakdown with per-status sections; McpStatusBar migrated from `getHealth` polling to `ServerDataCache.onDidRefresh`. |
| 11.5 | ✅ Add Server webview form | Replace sequential InputBox prompts with a single form, auto-detect transport type. |
| 11.6 | ✅ SAP grouping toggle | Group SAP systems by SID with colored VSP/GUI icons, opt-in via settings. |
| 11.7 | ✅ Add SAP System flow | Webview form: SID + component checkboxes + absolute-path VSP/GUI executables. |
| 11.8 | ✅ Settings consolidation | All 7 settings already under `mcpGateway.*` (apiUrl, autoStart, daemonPath, pollInterval, sapGroupBySid, slashCommandsEnabled, slashCommandsPath); all `getConfiguration('mcpGateway')` calls, all commands, and storage key `mcpGateway.credentialIndex` verified namespace-consistent (audit 2026-04-16, no code changes). |
| 11.9 | ✅ Slash command generation | Auto-generate `.claude/commands/<server>.md` on server start, delete on stop. |

### Phase 12 — Auth + KeePass (v1.2.0)

Detailed plan `docs/PLAN-main.md:115-354` (audited 2026-04-17, architect APPROVE_WITH_REFINEMENTS + dev-lead + lead-auditor 2 cycles + specialist-auditor 2 cycles, zero MEDIUM+ findings).

**Phase 12.A COMPLETE 2026-04-18** — commits `30bffd9` (ADR-0003), `6686cd2` (daemon + mcp-ctl), `4e075bf` (VS Code extension), `a168647` (PAL gate fixes). Bearer token auth is live on daemon REST + SSE + MCP transports, `mcp-ctl` carries the Authorization header, the extension shares one `buildAuthHeader` helper between `GatewayClient` and `LogViewer`, and a 22-assertion integration test (8-case MCP transport policy matrix + auth-before-csrf ordering + intentional-non-coverage assertions + X-Forwarded-For spoof regression + Sec-Fetch-Site cross-site deny + 401 no-store headers) guards the policy matrix. PAL codereview (gpt-5.2-pro, security mode) on the phase landed 1 CRITICAL (`X-Forwarded-For` bypass via `middleware.RealIP`), 3 HIGH, 3 MEDIUM — all fixed in `a168647`. Bearer-without-TLS WARN is in place for the `allow_remote=true` + no-TLS startup path until Phase 13.B lands TLS.

**Phase 12.B COMPLETE 2026-04-18** — commits `7b1e52f` (T12B.1–T12B.6) + follow-up PAL fix (1 CRITICAL Found=0 JSON contract break, 3 HIGH mcp-ctl path / server-name validation / stdin password []byte hygiene, 5 MEDIUM stdin cap / exit-code typing / env isolation / per-entry SecretStorage loop / summary UX). KeePass credentials now flow from KDBX → `mcp-ctl credential import --json --password-stdin` (argv-array exec, no shell, stdout never logged) → extension parses the stable version=1 JSON contract → `applyImportedCredentials` writes per-server into SecretStorage with partial-failure tolerance and malformed-name rejection. 478 extension tests passing (+7 new: 4 initial keepass + 3 PAL regression). 6 new Go assertions cover the JSON contract golden shape + version guard + Found=0 + password-stdin piping / mutex / empty-rejection.

**Phase 12.A — Bearer Token Auth (v1.2.0 Go daemon + mcp-ctl + GatewayClient + LogViewer)**

| # | Task | Description |
|---|------|-------------|
| 12.A.0 | ADR-0003 | Policy matrix + Windows DACL rationale + token lifecycle + CSRF scope + escape-hatch semantics |
| 12.A.1 | Token package | `crypto/rand` 32 bytes, base64url, atomic tmp+rename, read-if-exists persistence |
| 12.A.2 | Windows DACL | Split `auth_file_windows.go` (DACL SID restrict, deny-by-default) + `auth_file_other.go` (Unix 0600); tiered CI+integration tests |
| 12.A.3a | BearerAuthMiddleware | chi middleware, `crypto/subtle.ConstantTimeCompare`, 401 with `hint` field guidance |
| 12.A.3b | Middleware wiring | Global `r.Use` → explicit `/api/v1` groups; auth first (cheap 401), then csrf; CSRF scope narrowed to `/api/v1` |
| 12.A.3c | MCP transport policy | loopback-only default; Bearer when `allow_remote=true`; 8-case policy matrix. Open question resolved 2026-04-18 — Claude Desktop/Code/Cursor all support custom `Authorization` headers via `mcpServers[].headers` in local config files (MCP spec 2025-03-26+). T12A.3c is single-path. |
| 12.A.3d | `/logs` SSE auth | Wrap SSE group with `auth.Middleware` before `middleware.Throttle(20)` (auth-first DoS hardening) |
| 12.A.4 | Startup guards | Refuse `--no-auth + allow_remote` without `MCP_GATEWAY_I_UNDERSTAND_NO_AUTH=1`; Bearer-without-TLS WARN |
| 12.A.5 | Atomic token write ordering | `LoadOrCreate` before `http.Server.Serve`; token file mtime < first request |
| 12.A.6 | Go auth helper | `internal/auth/client.go` `BuildHeader(pathOrEnv)`; shared by daemon self-test + mcp-ctl |
| 12.A.7 | mcp-ctl auth wiring | Bearer on all HTTP subcommands; `MCP_GATEWAY_AUTH_TOKEN` env override; 401 error messaging |
| 12.A.8 | Extension `GatewayClient` auth | Read token (env > file), bounded ENOENT retry (5×200ms), `buildAuthHeader()` shared helper |
| 12.A.9 | Extension `LogViewer` auth | Import `buildAuthHeader()` from T12A.8; attach header to `/logs` requests |
| 12.A.10 | Auth token path setting | `mcpGateway.authTokenPath` in `package.json`; platform-resolved default |
| 12.A.11 | First-start migration UX | Token generation on fresh install; permission-wrong WARN (no auto-fix); extension reload-token on 401 |
| 12.A.12 | Logging hygiene pass | Grep for token/Authorization leaks; redact to `Bearer ***`; capture-logs test |
| 12.A.13 | Auth integration test matrix | All routes: 401 without Bearer, 200 with; csrf+auth ordering; intentional non-coverage docs (`/mcp`, `/sse`, `/api/*` redirect exempt) |
| 12.A.GATE | Per-Phase Gate | `go test ./...` + `go vet ./...` + `npm test` + PAL codereview+thinkdeep (zero MEDIUM+) + `npm run deploy` + VSIX commit |

**Phase 12.B — KeePass Credential Push (TS extension + 2 Go tweaks)**

| # | Task | Description |
|---|------|-------------|
| 12.B.1 | `--json` flag | `mcp-ctl credential import --json` outputs stable JSON contract; golden test |
| 12.B.2 | `--password-stdin` flag | Mutual-exclusive with `--password-file`; no-TTY stdin password piping; clears buffer |
| 12.B.3 | `keepass-importer.ts` | `child_process.execFile` with argv-array, 1MB maxBuffer; never log stdout/stderr |
| 12.B.4 | SecretStorage dual-write | Partial-failure aware; merge-on-write `_addToIndex` for multi-window safety (concurrent test required) |
| 12.B.5 | Command + settings registration | `package.json` `mcpGateway.importKeepassCredentials` + `keepassPath`/`keepassGroup` settings |
| 12.B.6 | Extension E2E test | Full flow: KeePass → `mcp-ctl --json` → token (T12A.8) → PATCH authed → SecretStorage multi-window scenario |
| 12.B.GATE | Per-Phase Gate | `go test ./...` + `go vet ./...` + `npm test` + PAL codereview+thinkdeep (zero MEDIUM+) + CHANGELOG.md v1.2.0 entry + `npm run deploy` + VSIX commit |

### Phase 13 — Security Hardening (v1.3.0) ✅ COMPLETE

Commits `a845a78` (initial) + `c242b35` (PAL gate fixes — 6 HIGH + 2 MEDIUM resolved: manager error-path process groups, Stop/session deadlock guard, watcher Rename/Remove + loop-var capture, IPv6 bind address, JWT redaction pattern, context-preserving redaction, comment accuracy).

| # | Task | Description |
|---|------|-------------|
| 13.1 | ✅ POSIX process groups (F-5) | `Setpgid=true` + group-aware SIGTERM/SIGKILL helpers, applied to Stop() and both error paths; Windows unchanged (Job Objects already handle grandchildren). |
| 13.2 | ✅ Config watcher race (F-6) | `sync.Mutex` around `onChange` with double-checked `ctx.Err()`; also handles Rename/Remove for atomic-save editors; regression test asserts peak in-flight = 1. |
| 13.3 | ✅ TLS support (F-7) | `GatewaySettings.TLSCert/KeyPath`; `ServeTLS` branch; **non-loopback + Bearer + no TLS refuses to start** (cleartext tokens impossible by design). IPv6 bind address now handled via `net.JoinHostPort`. |
| 13.4 | ✅ Log redaction (F-9) | `internal/logbuf/redact.go` ordered-match pipeline: Authorization Bearer, bare Bearer, api/access/secret/auth keys, password, AWS AKIA, GitHub PAT (ghp/gho/ghu/ghs/ghr), JWTs (three-segment), generic 32+ char base64url. Context-bearing patterns preserve field name. Ring.Write scrubs on entry — SSE and history both sanitised. 10 regression tests. |

### Phase 14 — Community & CI (v1.4.0) ✅ COMPLETE

Shipped in commit `29e6fc2`: `SECURITY.md` (responsible disclosure, 30-day target, scope boundaries), `.gitleaks.toml` (project-specific allowlist + committed-token rule), `gitleaks` job in `.github/workflows/ci.yml`, README Security section updated for auth / TLS / redaction / KeePass import.

Deferred work split into dedicated plans:
- `docs/PLAN-catalogs.md` (v1.5.0) — server catalog, command catalog, catalog browse in Add Server webview, slash-command template enrichment. **ALL PHASES COMPLETE 2026-04-20** (CA → CB → CC → CD). **Phase CA complete 2026-04-19** — schemas (server + command, draft-07, $id v1), 5+5 seed entries, ajv strict-mode loader (TOCTOU-safe via `fs.promises.open` + bounded read, never throws, 1 MiB cap), 17 mocha tests, plain-JS cross-ref script, `lint:catalog` script, CI step (lint + VSIX-contents check), `.vscodeignore` negations for ajv runtime deps. Gate [thinkdeep PASS / codereview fallback to Sonnet → 0 blocking after fixes]. **Phase CB complete 2026-04-19** — `mcpGateway.catalogPath` setting (machine scope), "Choose from catalog" `<select>` in AddServerPanel webview, `init` postMessage carrying catalog entries, synchronous change-handler + pre-fill of name/target/env_keys/header_keys, host-side re-validation of `catalogId` (forged ids rejected before `client.addServer`), operator override / bundled fallback path resolution, `<script>`-laden catalog entries rendered safely via `textContent` / `.value` (never `innerHTML`). Added 11 new mocha cases (3 CB.0 baseline + 7 CB.5 catalog browse + 1 CB.4 manifest). 506 passing / 31 pre-existing failures (zero CB regressions). CB.GATE [PAL MCP unavailable → Sonnet 4.6 code-reviewer + general-purpose fallbacks]: 0 CRITICAL / 0 HIGH, 4 MEDIUM + 8 LOW (all fixed in-cycle). **Phase CC complete 2026-04-20** — `SlashCommandGenerator` now loads both catalogs lazily (one-shot per generator) via shared `resolveCatalogDir` helper extracted to `src/catalog-path.ts` (47-line helper; AddServerPanel refactored to import it, eliminating CB/CC drift risk). `buildContent(server)` emits MARKER + catalog template body when commands catalog has a matching `server_name`, or skeleton otherwise (pre-CC regression preserved). Allow-list `String.prototype.replaceAll` substitution of `${server_name}`/`${server_url}`; unknown `${var}` tokens survive literal. Config watcher on `mcpGateway.catalogPath` invalidates cache. 7 new mocha cases: enriched write, skeleton fallback, allow-list + unknown-literal + repeat-occurrence, user-edit preservation, entry-removal fallback, onDidChangeConfiguration hook, empty-url drift. 513 passing / 31 pre-existing failures (+7 passing, zero CC regressions). CC.GATE [PAL thinkdeep gpt-5.2-pro PASS 0 findings 152 s; PAL codereview timed out twice → Sonnet 4.6 code-reviewer fallback APPROVE 0 findings]. **Phase CD complete 2026-04-20** — README §Catalogs (layout, seed examples, never-network statement, operator override, 1 MiB cap, $id v1 pinning, below-line-1 limitation), CHANGELOG v1.5.0 entry (Added + Security; concurrent v15 session merged Breaking-config + Fixes + Hygiene + Tests + Documentation into same release block), VSIX rebuilt 560.01 KB / 579 files / 4 × docs/catalog/* bundled, final security codereview. CD.GATE [PAL thinkdeep gpt-5.2-pro PASS 0 findings 112 s / rev-861703ebf284; PAL codereview timed out twice → Sonnet 4.6 code-reviewer APPROVE_WITH_LOW, 4 LOW fixed in-cycle (D-1 schema env_keys/header_keys pattern tightening to match html-builder.ts comment assertion; D-2 path.join safety comment for SERVER_NAME_RE; D-3 reviewer-confirmed non-actionable; D-4 require-from-string false positive — dep IS ajv 8.18 runtime). Packaging deviation: `code --install-extension` local CLI bad-option failure; compile + vsce package succeeded; operators run `Developer: Reload Window` post-pull. Screenshot omitted as operator-manual follow-up (no VSCode runner in session). Tests held at 513 passing / 31 pre-existing failing]. **v1.5.0 catalogs track closed.**
- `docs/PLAN-v15.md` (v1.5.0 tail) — **ALL PHASES COMPLETE 2026-04-20** (15.A → 15.B → 15.C → 15.D). **Phase 15.A complete 2026-04-19** (`5c949ca`) — T15A.1 ConstantTimeCompare hygiene refactor + T15A.2a/b scanner 1 MiB cap (atomic pair, F-11 closed). **Phase 15.B complete 2026-04-19** (`be9bbe9`) — T15B.1 TLS self-signed success test (ServeTLS branch now exercised, CA→leaf chain in `t.TempDir()`, client pins CA in `RootCAs`), T15B.2 non-loopback+auth+no-TLS refusal wording pin, T15B.3 half-configured TLS defect fix + test (both orderings, deliberate wording naming both keys — **breaking-config change**, CHANGELOG quotes both variants verbatim). Gate [C+C-internal] PASS, 3 cycle-1 findings (M-01, M-02, L-01) all fixed in-cycle. **Phase 15.C complete 2026-04-19** (`22f94c3`) — T15C.0 spike DEFERRED (pre-push hook blocked throwaway branch to public remote; spike report at `docs/spikes/2026-04-19-windows-latest-impersonate.md`). T15C.1 `internal/auth/token_perms_integration_windows_test.go` (`integration && windows` build tag, `LogonUserW` + `ImpersonateLoggedOnUser` via advapi32 LazyDLL, `runtime.LockOSThread` pin, combined `RevertToSelf`+`UnlockOSThread` defer so revert-failure terminates the locked thread, skip when `MCPGW_TEST_USER/PASSWORD` absent). T15C.2 manual-protocol branch: new `Makefile` with `test-integration-windows` target (fail-fast env-var guard) + README §Testing tiers three-tier table + elevated-PowerShell operator protocol. Gate [PAL gpt-5.1-codex + gpt-5.2] PASS, 3 findings (HIGH LockOSThread + MEDIUM silent-skip + LOW shell-mix) all fixed in-cycle; precommit MEDIUM teardown-leak fixed. **Phase 15.D complete 2026-04-20** (`253ce52` — PLAN-v15 GATE flip; CHANGELOG/README content merged via parallel catalogs commit `864c5d5`) — T15D.1 CHANGELOG v1.5.0 entry with Hygiene + Breaking-config + Fixes + Tests + Documentation + ROADMAP sub-sections (both TLS error-string variants quoted verbatim), T15D.2 README Testing tiers + Assurance-levels paragraph separating Windows (DACL shape vs OS-kernel enforcement) from Linux/macOS (POSIX 0600). Recovery-session closure after original `/run v15` session died mid-pipeline with lease spawn_token lost. Gate [thinkdeep PAL PASS / codereview Sonnet 4.6 fallback / precommit PAL PASS]: 1 HIGH + 1 MEDIUM + 1 LOW all fixed in-cycle; lead-auditor final pass APPROVE 0 findings.

**v1.5.0 released** — 9 commits pushed to origin/main 2026-04-20 (`253ce52..953c3cb`).

---

## Roadmap — v1.4.0 shipped

PLAN-main.md is complete as of commit `29e6fc2`. v1.4.0 delivers Bearer auth (12.A), KeePass credential push (12.B), security hardening (13), and community/CI foundations (14).

Next plans:
- ✅ `docs/PLAN-catalogs.md` — v1.5.0 catalog UX **[COMPLETE 2026-04-20 — commits 54f8c16, c49e6ef, 6e70dbd, 864c5d5]**
- ✅ `docs/PLAN-v15.md` — v1.5.0 tail items **[COMPLETE 2026-04-20 — commits 5c949ca, be9bbe9, 22f94c3, 253ce52]**
- **`docs/PLAN-16.md` — Phase 16 Claude Code Integration (v1.6.0) [PLAN APPROVED 2026-04-20 — audit APPROVE with 10 findings fixed in-cycle; PAL MCP unavailable → internal self-audit per CLAUDE.md fallback, documented in `docs/REVIEW-16.md`]** — Closes 3 prior-audit findings: HIGH bootstrap gap Claude Code ↔ Gateway, MEDIUM tools/list caching per Issue #13646, MEDIUM `.claude/commands/*.md` semantic confusion. Ships: dual-mode gateway (aggregate `/mcp` + per-backend `/mcp/{name}` proxy), Claude Code Plugin packaging (`~/.claude/plugins/cache/mcp-gateway@*/` with regenerated `.mcp.json`), webview patch automation, dashboard "Claude Code Integration" panel with tri-state Patch+Channel status, `gateway.invoke` universal fallback tool, `mcp-ctl install-claude-code` bootstrap CLI, supported-versions map, ADR-0005, dogfood `.mcp.json` swap. 10 sub-phases (16.0 SPIKE → 16.9 docs), 74+ tasks. Critical path: 16.0 → 16.1 → 16.3 → 16.4 → 16.5 → 16.8 → 16.9.
  - **Phase 16.0 COMPLETE 2026-04-21** — Spike `docs/spikes/2026-04-20-reload-plugins-probe.md` (3 passes documented). Original PLAN-16.4 premise (`executeCommand("reload-plugins")`) invalidated by static analysis + live DevTools verification on Claude Code 2.1.114. Native `session.reconnectMcpServer(name)` discovered at Fiber depth=2 (same neighborhood as `session.setModel` in `porfiry-taskbar.js`). Live probe PASS: real reconnect on server `"pal"` resolved with `{type:"reconnect_mcp_server_response"}` in 5404ms, active chat preserved. Phase 16.4 redesign under **Alt-E** (native `reconnectMcpServer` via fiber walk; no `extension.js` patch, no toggle hack). T16.0.GATE PASSED — no rescope triggered, 16.4/16.5 stay in scope with narrower implementation.
  - **Phase 16.1 COMPLETE 2026-04-21** — Gateway dual-mode (aggregate `/mcp` + per-backend `/mcp/{backend}` proxy) shipped in 4 files: `internal/proxy/gateway.go` (struct refactor, `aggregateServer` + `perBackendServer` map + `serverMu` RWMutex, dual-mode `RebuildTools()`, `registerToolForBackend()` helper, `ServerFor(name)` accessor), `internal/proxy/gateway_proxy_test.go` (NEW, 4 subtests), `internal/api/server.go` (`mcpServerForRequest` dispatcher — exact `/mcp` → aggregate; `/mcp/{valid-name}` → per-backend or SDK 400; `/mcp/{invalid}` → aggregate fall-through; T16.1.5.a SDK-path verification documented inline — go-sdk v1.4.1 streamable.go is single-endpoint), `internal/api/server_proxy_test.go` (NEW, 4 subtests). T16.1.GATE PASSED: `go test ./...` 12/12 PASS incl. 8 new Phase 16.1 tests (proxy 89.2% cov, api 74.8%); `go vet` clean; PAL thinkdeep async (gpt-5.1-codex) PASS 0 findings 98s; PAL codereview gpt-5.1-codex (internal fallback after external PAL timeout per CLAUDE.md) PASS 0 findings. Backward-compatible by construction: `Gateway.Server()` returns `aggregateServer`, existing `/mcp` clients unaffected. `mcpTransportPolicy` applied uniformly across `/mcp` and `/mcp/*` paths (bearer-required + loopback-only + Sec-Fetch-Site CSRF guard identical on both surfaces). Next: Phase 16.2 plugin packaging (`.claude-plugin/plugin.json` + `.mcp.json` regen + local marketplace).
  - **Phase 16 COMPLETE 2026-04-22** — `/finish` sweep closed the full 10-sub-phase track. Pipeline `checkpoint-finish-44f45055` ran architect critical-analysis → lead-auditor → specialist-auditor → doc-writer → precommit. Architect surfaced 5 findings (A-FIN-01..05): 2 HIGH (os.Setenv parent-env leak + wrong env var names in `runApplyPatch`), 1 MEDIUM (dead `'J'` FailureMode), 2 LOW (config_override round-trip gap, docs literal vs const). All 5 FIXED IN-CYCLE in commit `983a0da`. Specialist-auditor independent sweep (4 domains: security / concurrency / contract / cross-phase) — 0 new findings. Audit verdict: **APPROVE**. All 10 sub-phase GATE markers (T16.0.GATE through T16.9.GATE) remain `[x] PASSED`. T16.9.4 live `.mcp.json` dogfood swap explicitly DEFERRED to operator follow-up (documented in PLAN-16 + CHANGELOG). Full Phase 16 ships v1.6.0. Total scope delivered: 40 files changed, +9510 insertions (-100 deletions) across 19 commits (17 pre-finish + 983a0da fix + doc-writer commit to follow). Next track: Phase 17 candidates (SIGHUP wrapper, stdio transport reload, plugin marketplace publication) + v16-candidates backlog.

  - **Phase 16.4 COMPLETE 2026-04-22** — Claude Code webview patch (Alt-E native reconnect) consolidated from abandoned concurrent pipeline `feature-b8f2decf` as commit `e8de700` (5 files, +1863 lines). NEW: `installer/patches/porfiry-mcp.js` (464 lines — fiber walk + MutationObserver re-mount resilience; state machine unknown→discovering→ready→lost P4-02; CONFIG block with DEBOUNCE_WINDOW_MS=10s + DEBOUNCE_FORCE_FIRE_COUNT=10 + AWAITING_DISCOVERY_QUEUE_MAX=16 + INITIAL_SKEW_MAX_MS + CONFIG_OVERRIDE_RANGES SP4-L2 bounds; singleflight guard; 6-path-pattern error scrub SP4-M2 + stack-frame strip; heartbeat POST every 60s with jitter; action polling every 2s; probe handler calling `reconnectMcpServer` with fabricated sentinel server name expecting "Server not found" rejection as GREEN success signal); `installer/patches/apply-mcp-gateway.sh` (161 lines, mode 755 — awk-v placeholder substitution; idempotent marker check; index.js.bak backup preserve-once; chmod 600 REVIEW-16 L-03; token validator `^[A-Za-z0-9_\-\.]+$` SP4-cross; placeholder-survival post-check); `installer/patches/apply-mcp-gateway.ps1` (191 lines — PowerShell parity + `icacls /inheritance:r /grant:r` DACL mirror of Go SDDL `D:P(A;;FA;;;<SID>)`; `[System.IO.File]::WriteAllBytes` for byte-safe UTF-8 no-BOM; em-dash characters avoided for PS 5.1 non-ASCII rejection); `installer/patches/porfiry-mcp.test.mjs` (1036 lines — 17 PLAN-16 test cases + 5 pure-helper units + 3 regression tests from PAL findings, all 24/24 pass in 241 ms); `configs/supported_claude_code_versions.json` (9 keys byte-identical to /compat-matrix FROZEN contract; seed values from 2026-04-21 Alt-E spike: min=2.0.0, max_tested=2.5.8, alt_e_verified_versions=["2.1.114"], observed_fiber_depths={"2.1.114":2}, observed_reconnect_latency_ms_p50=5400). T16.4.GATE PASSED (during feature-b8f2decf): `node --check` + `bash -n` + `powershell Parser::ParseFile` all clean; `node --test porfiry-mcp.test.mjs` → 24/24 pass 241ms; `mcp__pal__codereview` gpt-5.1-codex 5 findings (1 HIGH + 2 MEDIUM + 2 LOW) all FIXED IN-CYCLE; `mcp__pal__thinkdeep` gpt-5.2-pro 6 findings (2 HIGH + 2 MEDIUM + 2 LOW) all FIXED IN-CYCLE. Total 11 findings fixed with 3 new regression tests. Spike T16.0 sign-off valid for CC 2.1.114 at fiber depth 2. `shellcheck` deferred to CI (not installed on build host). Consolidation into git done during `/finish` when the authoring pipeline was confirmed exited (list_active_pipelines returned empty).

  - **Phase 16.9 COMPLETE 2026-04-22** (docs slice — live `.mcp.json` dogfood swap T16.9.4 deferred to operator follow-up) — Docs + ADR-0005 + CHANGELOG v1.6.0 + dogfood-smoke CI shipped in 7 files (commit `4e39e84`, +419/-11 lines). NEW: `docs/ADR-0005-claude-code-integration.md` (hybrid dual-mode + plugin + Alt-E webview-patch rationale, 8 rejected alternatives enumerated with reasoning), `.github/workflows/dogfood-smoke.yml` (REVIEW-16 L-04 — starts gateway with repo config, waits for /health, asserts ≥3 running backends via curl+jq, on-PR trigger for paths `.mcp.json` / `config.json` / `configs/` / `cmd/mcp-gateway/` / `internal/`). MODIFIED: `CHANGELOG.md` v1.6.0 entry; `README.md` §"Connecting Claude Code to the Gateway" (two-line install + auto-reload opt-in + manual `/mcp` panel Reconnect fallback + dashboard shortcut) + §"Commands vs MCP servers" (closes the `.claude/commands/*.md` semantic-confusion class, issue #16143); `vscode/mcp-gateway-dashboard/src/slash-command-generator.ts` (new DISCLAIMER_LINE_2 + DISCLAIMER_LINE_3 constants + HEADER bundling; both `buildContent` and `skeletonTemplate` emit the 3-line header); slash-command test pins the new 3-line shape. T16.9.4 (live repo `.mcp.json` swap to dogfood through the gateway) DEFERRED to operator — the swap needs Phase 16.4 feature-b8f2decf webview patch landed first, and running it from inside the /run loop risks breaking the current session's MCP access. T16.9.GATE PASSED for this scope: `npm run compile` clean; scoped mocha `src/test/slash-command-generator.test.ts + claude-code-*.test.ts` → 86 pass 620 ms (includes new 3-line-header pinning assertions). PAL cross-validation deferred to `/finish` sweep (REVIEW-16.md still owned by concurrent feature-b8f2decf). Next: once feature-b8f2decf + feature-46b81e33 merge, `/finish` sweeps the full Phase 16 audit trail + T16.9.4 dogfood swap.

  - **Phase 16.8 COMPLETE 2026-04-22** — `mcp-ctl install-claude-code` bootstrap CLI shipped in 4 files (commit `46d72ac`, +781 lines). NEW: `cmd/mcp-ctl/install_claude_code.go` (cobra subcommand with flags `--mode|--scope|--no-patch|--dry-run|--refresh-token|--check-only`; LIFO rollback stack; `commandRunner` abstraction for test injection; exit codes 0 ok / 1 usage / 2 gateway-down / 3 drift-unresolved / 4 rollback-executed; token passed to apply script via `GATEWAY_AUTH_TOKEN` env var — never argv); `cmd/mcp-ctl/install_claude_code_test.go` (11 subtests + 1 Windows-skip) covering dry-run, gateway-down exit 2, missing-claude-CLI error, empty-token rejection, invalid-flag combos, `--check-only` aligned/drifted/missing-plugin (REVIEW-16 M-03), plugin-sync 409 → rollback (POSIX-only; Windows apply-script existence check blocks that path), `--no-patch` skip, applyScriptPath Windows/POSIX variant. MODIFIED: `cmd/mcp-ctl/main.go` registers the subcommand with `skipClientAnnotation`. Constants renamed `installExit*` to avoid collision with existing `exitError=1` const. T16.8.GATE PASSED: `go test ./cmd/mcp-ctl/` → ok 11.3 s; `go build ./...` + `go vet` clean. PAL cross-validation deferred to `/finish` sweep because concurrent `feature-b8f2decf` pipeline still owns `docs/REVIEW-16.md`. Next: Phase 16.9 docs + dogfood.

  - **Phase 16.7 COMPLETE 2026-04-22** — Integration tests E2E (patch chain + CORS) shipped in 5 files (commit `11e1a6c`, +611 lines). NEW: `internal/api/integration_phase16_test.go` (build tag `integration`) with 3 subtests (FullPatchChain: add→regen→action→ack→remove→follow-up; ProbeTriggerRoundTrip: nonce correlation end-to-end; PluginSyncReturnsStatus: FROZEN contract shape); `internal/api/integration_cors_test.go` (no build tag, always-on) with 5 subtests (preflight allow/deny + actual-post echo/omit + preflight-before-auth-ordering regression guard for REVIEW-16 L-02); `docs/TESTING-PHASE-16.md` 4-tier test documentation with prerequisites + run commands + expected counts + manual VSCode smoke checklist; Makefile target `test-integration-phase16`. T16.7.3 node webview harness DEFERRED to Phase 16.4 close-out (depends on `installer/patches/porfiry-mcp.js` owned by concurrent `feature-b8f2decf` pipeline; documented in TESTING-PHASE-16.md §Tier 4). T16.7.GATE PASSED: full `go test ./... -count=1` → 14/14 packages green; `go test -tags=integration ./internal/api/... -run TestIntegration_Phase16` → 3/3 pass (total 25.39 s); `go test ./internal/api/... -run TestIntegration_CORS` → 5/5 pass (3.06 s). PAL thinkdeep cross-validation deferred to `/finish` sweep due to REVIEW-16.md concurrency with feature-b8f2decf. Next: Phase 16.8 mcp-ctl install-claude-code CLI.

  - **Phase 16.5 COMPLETE 2026-04-22** — Dashboard Claude Code Integration panel shipped in 10 files (commit `4c7b54e`, +2019 lines). NEW module `vscode/mcp-gateway-dashboard/src/claude-code/`: `types.ts` (FROZEN v1.6.0 contract types), `status.ts` (pure failure-mode state machine — 12 modes A/B/C/D/F/G/H/I/J/K/L/M with Mode E obsoleted under Alt-E + P4-05 counter reset + P4-06 boundary rules + P4-09 saturation gate + SP4-L2 config_override bounds), `diagnostics.ts` (Copy-diagnostics report builder with Alt-E metrics: p50/p95 latency over non-idle samples, fiber depth history newest-first, dedup recent errors), `patch-installer.ts` (platform-dispatch wrapper around apply-mcp-gateway.sh/.ps1 — REVIEW-16 M-04; token via env var never argv). NEW webview `src/webview/claude-code-panel.ts` (10 s status poller, CSP nonce, error fallback synthesises Mode H). NEW tests `src/test/claude-code-{status,diagnostics}.test.ts` (55 subtests covering every boundary + no-Mode-E regression guard). MODIFIED: `src/extension.ts` (`mcpGateway.showClaudeCodeIntegration` command + auth-token injection via `buildAuthHeader`), `package.json` (command manifest). Scope-narrowing (documented in PLAN-16): T16.5.2 `[Activate]` plugin-marketplace install flow deferred to Phase 16.8 CLI (authoritative install path — webview has no `claude` in PATH); T16.5.6 `[Probe reconnect]` result display rides the heartbeat status poller since the FROZEN contract has no per-nonce GET. T16.5.GATE PASSED: `npm run compile` clean; scoped mocha `src/test/claude-code-*.test.ts` → 55 pass 147 ms; broader 194-test sample → 1 s; full `src/test/**/*.test.ts` excluding v16-4 tech-debt tests (gateway-client/log-viewer/commands) → 492 pass 2 s. PAL cross-validation deferred to final `/finish` sweep because `docs/REVIEW-16.md` is currently being modified by concurrent pipeline `feature-b8f2decf` (Phase 16.4 code-reviewer step). Commit 4c7b54e message holds review record until then. Next: Phase 16.7 integration tests (full-chain E2E + CORS) — Phase 16.4 & 16.6 still in flight on other windows and deliberately skipped by this /run.

  - **Phase 16.6 COMPLETE 2026-04-22** — `gateway.invoke` universal fallback tool + meta-tools + Instructions + cache-busting `serverInfo.version` shipped in 3 files. MODIFIED: `internal/proxy/gateway.go` (+ ~220 LOC — `aggregateImpl *mcp.Implementation` field retained so `RebuildTools` can mutate Version for cache-busting; `versionMu sync.RWMutex` guards our writes + `ServerInfoVersion()` reads; `mcp.NewServer(impl, &mcp.ServerOptions{Instructions: gatewayInstructions})` wires the initialize-response guidance; `registerGatewayBuiltins()` adds three aggregate-only tools: `gateway.invoke` (universal fallback invoker, requires {backend, tool, args}), `gateway.list_servers` (runtime topology `[{name, status, transport, tool_count, health, uptime_seconds}]`, sorted), `gateway.list_tools` (map keyed by backend with `[{name, namespaced, description, inputSchema}]`, optional `server` filter); `computeTopologyVersion(baseVersion, sortedBackends, toolCount)` returns `baseVersion + "+" + hex[:4]` SHA-256 over `names|count`; RebuildTools tail pre-sorts and updates Version on every topology change with documented race trade-off; `serverSummary` + `toolSummary` types carry SCHEMA-FREEZE v1.6.0 markers). NEW: `internal/proxy/gateway_invoke_test.go` (~350 LOC — 11 tests: HappyPath with real session via `mcp.NewInMemoryTransports`, StaleToolsCache_Fallback regression, UnknownBackend, MissingRequiredArgs, ListServers with running+stopped+degraded, ListTools with/without filter, ServerInfoVersionChangesOnTopology four scenarios, ComputeTopologyVersion_Invariants, GatewayBuiltins_NotOnPerBackend scope invariant, GatewayInstructions_Surfaced). ADDED: `lifecycle.Manager.SetSession(name, *mcp.ClientSession)` test helper mirroring existing `SetStatus`/`SetTools` pattern. T16.6.5 (compat-matrix endpoint) already delivered in Phase 16.3 as PAL-CONTRACT-3 — no new work. T16.6.GATE PASSED: `go test ./... -count=1` 15/15 packages green, 11 new proxy tests PASS in 1.5s, 0 regressions; `go build ./...` + `go vet ./...` clean. PAL codereview gpt-5.1-codex external-expert (continuation `d04c3234`): 3 findings — **1 HIGH (CR-16.6-01 `handleGatewayInvoke` pre-rejected calls when tool missing from `entry.Tools`, defeating the fallback intent) + 1 MEDIUM (CR-16.6-02 no regression test for stale-cache path) + 1 LOW (CR-16.6-03 `uptime_seconds=0` semantics not documented) — ALL FIXED IN-CYCLE** (Tools scan removed; regression test added; comment expanded). PAL thinkdeep gpt-5.2-pro was unresponsive across two continuation rounds despite fully-embedded context; internal validation (`use_assistant_model=false`) completed with very_high confidence — six invariants (A determinism, B discrimination, C 32-bit hash rate, D schema stability, E race-on-Version, F compat-matrix forward-compat) all HOLD with two documented limitations (B narrow collision on simultaneous add+remove preserving total; E theoretical race on two-word string store — alternative is session-severing server rebuild). One action item applied: SCHEMA-FREEZE v1.6.0 markers on `serverSummary`/`toolSummary` types. `-race` deferred to CI (no gcc on build host). Full audit trail in `docs/REVIEW-16.md` §"Phase 16.6 Code Review". Closes the final code-delivery item in Phase 16; 16.7/16.8/16.9 already landed. Next: `/finish` sweep for the full Phase 16 audit trail + T16.9.4 dogfood `.mcp.json` swap.

  - **Phase 16.3 COMPLETE 2026-04-22** — Gateway REST endpoints + `patchstate` package shipped in 6 files (commit `a7521fa`, +2063 lines). NEW: `internal/patchstate/state.go` (heartbeats + FIFO pending-actions + probe-results, disk persistence via atomic tmp+rename 0600, TTL cleaner goroutine, FlushPersists graceful-shutdown hook, AggregatePluginServerName const enforcing P4-08 invariant), `internal/patchstate/state_test.go` (20 subtests including fake-clock TTL, debounce, FIFO + ack, persistence round-trip, cursor-miss fallback regression, corrupt-file tolerance, concurrent-access stress), `internal/api/claude_code_handlers.go` (8 endpoints under `/api/v1/claude-code/*`: POST /patch-heartbeat + GET /patch-status + GET /pending-actions + POST /pending-actions/{id}/ack + POST /probe-trigger + POST /probe-result + POST /plugin-sync + GET /compat-matrix; claudeCodeCORS middleware runs BEFORE authMW per REVIEW-16 L-02; in-memory token-bucket rate limiter with amortized eviction), `internal/api/claude_code_handlers_test.go` (15 subtests covering round-trip + bearer + CORS + OPTIONS preflight + debounce + persistence + 429 + Retry-After + contract-compliance). MODIFIED: `internal/api/server.go` (patchState + heartbeatLimiter + pendingActionsLimiter + patchStatusLimiter fields, SetPatchState, InitClaudeCodeLimiters, TriggerPluginRegen enqueues reconnect on successful regen, /api/v1/claude-code route group wiring), `cmd/mcp-gateway/main.go` (patchstate.New + Load + StartCleaner(30s) wiring, FlushPersists before lm.StopAll on graceful shutdown for M-01 durability). T16.3.GATE PASSED: `go test ./... -count=1` 14/14 PASS (35 new subtests, 0 regressions); `go build ./...` + `go vet ./...` clean; PAL MCP went offline mid-phase → Agent sub-agent fallback round 1 (2 HIGH + 3 MEDIUM + 2 LOW) + round 2 (1 MEDIUM + 2 LOW) all fixed in-cycle; post-recovery `mcp__pal__codereview` gpt-5.1-codex round surfaced 3 findings (PAL-CR1 false positive on Go 1.25 `WaitGroup.Go`, PAL-CR2 shared patch-status limiter, PAL-CR3 no FlushPersists on shutdown) + 3 FROZEN-contract endpoint gaps (probe-trigger, plugin-sync, compat-matrix — spec enriched by parallel cross-window-bus pipeline `3a73780` during the outage) — ALL fixed in-cycle. `mcp__pal__precommit` final sign-off. Full audit trail in `docs/REVIEW-16.md` §"Phase 16.3 review" (16 findings, all Fixed). FROZEN v1.6.0 API contract in `docs/api/claude-code-endpoints.md` now matches implementation 1:1. Next: Phase 16.4 webview patch (`installer/patches/porfiry-mcp.js` + apply-mcp-gateway.sh/.ps1).

  - **Phase 16.2 COMPLETE 2026-04-21** — Claude Code plugin packaging + `.mcp.json` regen + local marketplace shipped in 9 files. NEW: `installer/plugin/.claude-plugin/plugin.json` (userConfig `gateway_url` + `auth_token` with `sensitive:true` → OS keychain), `installer/plugin/.mcp.json` (GENERATED-banner stub), `installer/plugin/README.md` (operator docs + rotation runbook), `installer/marketplace.json` (one-command `claude plugin install mcp-gateway@mcp-gateway-local`), `internal/plugin/regen.go` (Regenerator with `sync.Mutex` per REVIEW-16 M-02, atomic `CreateTemp`+`Sync`+`Chmod`+`Rename` + pre-write `.mcp.json.bak` backup only when prior differs, idempotent no-op on byte-equal, JSON round-trip validation pre-write, Disabled-backend exclusion, per REVIEW-16 L-05 "gateway OWNS the file" contract), `internal/plugin/discover.go` (`$GATEWAY_PLUGIN_DIR` env → `~/.claude/plugins/cache/mcp-gateway@*` glob with lex-max preference and non-directory filter → `ErrPluginDirNotFound`), `internal/plugin/regen_test.go` (14 subtests covering atomicity, idempotency, backup contract, JSON validity, disabled exclusion, empty-dir rejection, default placeholder, concurrency n=10, and all 5 Discover branches). MODIFIED: `internal/api/server.go` (`Server.pluginRegen + pluginDir` fields, public `SetPluginRegen` setter, public `TriggerPluginRegen` with deep-copy snapshot under `cfgMu.RLock` — PAL-CR-H1 fix — called from `handleAddServer` / `handleRemoveServer` / `handlePatchServer`-on-Disabled-toggle), `cmd/mcp-gateway/main.go` (`plugin.Discover` + `SetPluginRegen` at startup, bootstrap `TriggerPluginRegen` after initial `lm.StartAll` / `gw.RebuildTools` — PAL-TD-GAP2 fix; config-watcher reload also calls `TriggerPluginRegen` after `UpdateConfig` / `RebuildTools` — PAL-TD-GAP1 fix). T16.2.GATE PASSED: `go test ./... -count=1` 13/13 PASS (14 new `internal/plugin/` subtests, 0 regressions); `go build ./...` clean; `mcp__pal__codereview` gpt-5.1-codex external-expert found 1 HIGH (PAL-CR-H1 snapshot pointer aliasing) FIXED in-cycle via deep-copy under `cfgMu.RLock`; `mcp__pal__thinkdeep` gpt-5.1-codex external-expert found 2 MEDIUM (PAL-TD-GAP1 config-reload + PAL-TD-GAP2 startup-bootstrap) FIXED in-cycle via public `TriggerPluginRegen` wired at both missed sites. 4 additional lifecycle gaps classified LOW/out-of-scope (plugin-installed-post-start, plugin-dir-vanished, cache-scheme-change, token-rotation) tracked in 16.7/16.8/16.9. Full audit in `docs/REVIEW-16.md` §"Phase 16.2 implementation audit". Next: Phase 16.3 REST endpoints for patch integration.
  - **/check Pass 4 audit COMPLETE 2026-04-21** — checkpoint-check pipeline `checkpoint-check-2a570a35` audited commit 0f7cfd0 across 7 steps (qa-lead → architect → lead-auditor → specialist-auditor → backend-dev → doc-writer → qa-lead). 15 findings surfaced (1 HIGH, 8 MEDIUM, 6 LOW) from 3 audit perspectives: architect (9 spec-coherence issues), lead-auditor (1 PII cross-domain), specialist-auditor (4 concurrency/security + 1 cross-domain flag). **ALL 15 fixed in-cycle** as PLAN-16.md amendments (spec-level, no code yet). Notable fixes: SP4-M1 debounce-at-lost state-check (silent action-loss gap), SP4-M2 broadened PII scrub regex (container/CI paths), SP4-L2 raised config_override lower bounds (DoS-by-misconfiguration), SP4-cross T16.4.4 shell-injection hardening for token substitution, P4-01 probe-reconnect handler routing. PAL CV: `mcp__pal__thinkdeep gpt-5.1-codex` [C+O] full agreement on specialist findings; CV-gates at step boundaries auto-SKIPPED 3x due to PAL timeouts, Read-evidenced independent reviews substituted per CLAUDE.md fallback rule. Pass-4 verdict: APPROVE, zero errors remaining. Regression check: `go test ./...` 11 packages PASS, 0 failures.


| # | Task | Description |
|---|------|-------------|
| 14.1 | SECURITY.md | Responsible disclosure policy for contributors. |
| 14.2 | gitleaks in CI | Automated secret scanning in GitHub Actions on every push. |
| 14.3 | Server catalog | Built-in catalog of popular MCP servers (context7, pal, etc.) with auto-fill in Add Server. |
| 14.4 | Slash command catalog | Library of ready-made slash commands for common MCP servers. |

Detailed plan: `docs/PLAN-main.md` (audited [C+O] 2026-04-10, 0 MEDIUM+).
Full codebase audit: `docs/REVIEW-main.md` (audited [C+O] 2026-04-14, 13 findings fixed, 11 deferred to planned phases).

### Phase 11 Completion Summary (2026-04-16)

- **Status:** Phase 11 (Extension UX v1.1.0) — ALL sub-phases COMPLETE. Final commits: `8650505` (11.A) → `6d059e2` (11.B) → `ec3aae7` (11.C) → `44f0d7a` (11.D) → `991121f` (11.E) → `64f6057` (ROADMAP/REVIEW update). 11.8 audit 2026-04-16.
- **Unpushed commits on `main`:** `8650505`, `2a10e8b`, `6d059e2`, `ec3aae7`, `44f0d7a`, `991121f`, `64f6057` — push to GitLab (`git push origin main`) to publish v1.1.0 work.
- **Next plan:** Phase 12 (Auth + KeePass) — run `/phase 12` to create detailed task breakdown, then `/run main`.
- **PAL MCP status:** unavailable in this project; all cross-validation runs via sonnet sub-agent fallback per CLAUDE.md.
- **Known test-infra debt:** 31 pre-existing `GatewayClient` + `LogViewer` test failures need a local mock HTTP server — tracked as out-of-phase cleanup, not a Phase 11 blocker. The rebuilt VSIX (`vscode/mcp-gateway-dashboard/mcp-gateway-dashboard-latest.vsix`) must be installed manually via VSCode UI because `code --install-extension` fails on this machine's PATH.

---

## v1.6.0 Candidates (not yet planned)

| ID | Candidate | Origin |
|----|-----------|--------|
| v16-1 | **Hash-augmented slash-command marker** — tolerate operator edits below line 1 of generated `.claude/commands/<server>.md` files by using a per-entry content hash on the magic-header marker. Current marker is regenerated in full on each server `running` transition; edits below line 1 are silently overwritten. | Catalogs Phase CC known limitation (CHANGELOG v1.5.0 §Added) |
| v16-2 | **Windows CI runner for DACL enforcement** — revisit T15C.0 spike. Re-attempt only after the `spike/windows-impersonate` binary is fixed to add `runtime.LockOSThread` (latent bug noted in spike report) and the `.git/hooks/pre-push` anti-leak policy is re-evaluated for throwaway-branch CI spikes. | PLAN-v15.md T15C.0 DEFERRED |
| v16-3 | **Add Server catalog screenshot** — `docs/screenshots/catalog-dropdown.png` (live VSCode capture, wire into README Catalogs section). | Catalogs Phase CD commit body (deferred as operator-manual follow-up) |
| v16-4 | **31 pre-existing `GatewayClient` + `LogViewer` test failures** — stand up a local mock HTTP server so these tests can pass cleanly instead of being tracked as a baseline. | Phase 11 Completion Summary (tracked out-of-phase since 2026-04-16) |
| v16-5 | **Unified VSIX deploy reliability** — `code --install-extension` fails on this developer's machine with "bad option" even when compile + `vsce package` succeed; operator workaround is `Developer: Reload Window`. Investigate `.vscode/launch.json` + extension-install CLI path. | Catalogs Phase CD packaging deviation |
| v16-6 | **Auth token rotation** — currently `LoadOrCreate` persists the token indefinitely; no rotation command exists. Add `mcp-ctl auth rotate-token` and extension reload-on-401 refinements. | Post-v1.2.0 observation (not blocking v1.5.0) |
| v16-7 | **Catalog entry discovery** — optional fetch from an operator-provided signed URL for community catalog entries, gated by cosign signature verification (never auto-fetched). Open question: does the v1.5.0 "never fetched from network" guarantee hold if explicitly opted-in? | Architect note during catalogs CA planning (logged for future) |

None of these are committed scope. Pick based on user priority.

---

## Known Limitations (LOW, documented)

| ID | Description |
|----|-------------|
| F-10 | KeePass master password in Go string cannot be zeroed due to GC — acceptable for localhost. |
| F-11 | ~~bufio.Scanner 64KB line limit for stderr~~ — CLOSED 2026-04-19 in Phase 15.A (T15A.2a + T15A.2b). Both scanner sites (SSE client + stderr producer) raised to 1MB atomically; regression tests pin the cap. End-to-end log-line ceiling is now 1MB. |
| RF-3 | Gateway crash = all MCP servers lost — HA/clustering not planned (localhost daemon). |
